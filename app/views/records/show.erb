<% if flash.has?(:message)%>
<h1><%=flash[:message]%></h1>
<p>Click here to add to your collection<p>

<%end%>

<h2><%=@record.name%></h2>

<table>

  <tr>
    <td>Artist: </td>
    <td><%= @record.artist.name %></td>
  </tr>
  <tr>
    <td>Released: </td>
    <td><%= @record.release_year %></td>
  </tr>
  <tr>
    <td>Label: </td>
    <td><%= @record.label.name %></td>
  </tr>
  <tr>
    <td>Type: </td>
    <td><%= @record.record_type %></td>
  </tr>
  <tr>
    <td>Format: </td>
    <td><%= @record.format %></td>
  </tr>
<table>
  <form action = "/records/<%=@record.slug%>/edit">
    <input type = "submit" value = "Edit Song">
  </form>
